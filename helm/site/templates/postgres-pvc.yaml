apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc-{{ include "app.postgresVersion" . }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  resources:
    requests:
      storage: {{ .Values.postgres.storage }}
